node_modules/
.env
.env.*
!.env.example

# Runtime data — client records (incl. dashboard access tokens), captured
# leads, and the atomic-write temp/backup files. Never commit these.
data/
*.log
.DS_Store
